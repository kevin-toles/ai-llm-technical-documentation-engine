# ==============================================================================
# LLM Document Enhancer - Integration Environment Configuration
# ==============================================================================
# Reference: END_TO_END_INTEGRATION_WBS.md WBS 0.1.2
# Usage: Source this file or use with docker-compose:
#   docker-compose -f docker-compose.integration.yml --env-file .env.integration up -d
#
# Note: This file configures the integration test environment.
#       For production, use .env with appropriate values.
# ==============================================================================

# ==============================================================================
# SERVICE URLS (WBS 0.1.2.1 - 0.1.2.4)
# ==============================================================================

# Semantic Search Service (WBS 0.1.2.1)
SEMANTIC_SEARCH_URL=http://localhost:8081

# LLM Gateway Service (WBS 0.1.2.2)
GATEWAY_URL=http://localhost:8080
LLM_GATEWAY_URL=http://localhost:8080

# Qdrant Vector Database (WBS 0.1.2.3)
QDRANT_URL=http://localhost:6333

# Neo4j Graph Database (WBS 0.1.2.4)
NEO4J_URI=bolt://localhost:7687
NEO4J_URL=bolt://localhost:7687

# ==============================================================================
# DATABASE CREDENTIALS (WBS 0.1.2.5)
# ==============================================================================

# Neo4j credentials (must match docker-compose.integration.yml)
NEO4J_USER=neo4j
NEO4J_PASSWORD=devpassword

# ==============================================================================
# LLM PROVIDER CONFIGURATION (WBS 0.1.2.6)
# ==============================================================================

# Primary LLM Provider
LLM_PROVIDER=anthropic
LLM_GATEWAY_DEFAULT_PROVIDER=anthropic

# Anthropic Configuration
# Replace with your actual API key for integration tests
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-your-anthropic-api-key-here}
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
LLM_GATEWAY_DEFAULT_MODEL=claude-sonnet-4-5-20250929

# OpenAI Configuration (Optional - uncomment if using OpenAI)
# OPENAI_API_KEY=${OPENAI_API_KEY:-your-openai-api-key-here}
# OPENAI_MODEL=gpt-4-turbo

# ==============================================================================
# SERVICE CONFIGURATION
# ==============================================================================

# Gateway Settings
LLM_GATEWAY_ENV=integration
LLM_GATEWAY_PORT=8080
LLM_GATEWAY_LOG_LEVEL=INFO
LLM_GATEWAY_WORKERS=4
LLM_GATEWAY_REDIS_URL=redis://localhost:6379
LLM_GATEWAY_SEMANTIC_SEARCH_URL=http://localhost:8081
LLM_GATEWAY_AI_AGENTS_URL=http://localhost:8082
LLM_GATEWAY_RATE_LIMIT_REQUESTS_PER_MINUTE=60
LLM_GATEWAY_SESSION_TTL_SECONDS=3600

# Semantic Search Settings
SEMANTIC_SEARCH_PORT=8081
SBERT_MODEL=all-mpnet-base-v2
ENABLE_GRAPH_SEARCH=true
ENABLE_HYBRID_SEARCH=true

# AI Agents Settings
AI_AGENTS_PORT=8082
AI_AGENTS_LOG_LEVEL=INFO
ENABLE_CROSS_REFERENCE_AGENT=true

# ==============================================================================
# INTEGRATION TEST SETTINGS
# ==============================================================================

# Test timeouts (seconds)
TEST_TIMEOUT=30
HEALTH_CHECK_TIMEOUT=60

# Test data paths
TEST_DATA_DIR=./tests_integration/data
TEST_REPORTS_DIR=./reports
